"use client";import{useEffect,useRef,useState}from"react";import GameShell from"@/components/GameShell";import{gameBySlug}from"@/data/games";import{bestScore,getStored}from"@/lib/storage";type P={x:number;y:number};const N=20;export default function Game(){const canvas=useRef<HTMLCanvasElement>(null),snake=useRef<P[]>([]),food=useRef<P>({x:15,y:10}),dir=useRef<P>({x:1,y:0}),next=useRef<P>({x:1,y:0}),[score,setScore]=useState(0),[best,setBest]=useState(()=>getStored("best:snake",0)),[paused,setPaused]=useState(false),[over,setOver]=useState(false),touch=useRef<[number,number]>([0,0]);function reset(){snake.current=[{x:8,y:10},{x:7,y:10},{x:6,y:10}];food.current={x:15,y:10};dir.current={x:1,y:0};next.current={x:1,y:0};setScore(0);setOver(false);setPaused(false);draw()}function setDirection(x:number,y:number){if(x!==-dir.current.x||y!==-dir.current.y)next.current={x,y}}function draw(){const c=canvas.current;if(!c)return;const x=c.getContext("2d")!;x.fillStyle="#101828";x.fillRect(0,0,400,400);x.fillStyle="#ffcf4a";x.beginPath();x.arc(food.current.x*20+10,food.current.y*20+10,8,0,7);x.fill();snake.current.forEach((p,i)=>{x.fillStyle=i?"#8b75ff":"#b5ff7d";x.fillRect(p.x*20+2,p.y*20+2,16,16)})}useEffect(()=>{reset();const key=(e:KeyboardEvent)=>{const d:{[k:string]:P}={ArrowUp:{x:0,y:-1},w:{x:0,y:-1},ArrowDown:{x:0,y:1},s:{x:0,y:1},ArrowLeft:{x:-1,y:0},a:{x:-1,y:0},ArrowRight:{x:1,y:0},d:{x:1,y:0}};if(d[e.key]){e.preventDefault();setDirection(d[e.key].x,d[e.key].y)}};addEventListener("keydown",key);return()=>removeEventListener("keydown",key)},[]);useEffect(()=>{if(paused||over)return;const id=setInterval(()=>{dir.current=next.current;const h={x:snake.current[0].x+dir.current.x,y:snake.current[0].y+dir.current.y};if(h.x<0||h.y<0||h.x>=N||h.y>=N||snake.current.some(p=>p.x===h.x&&p.y===h.y)){setOver(true);return}snake.current.unshift(h);if(h.x===food.current.x&&h.y===food.current.y){setScore(s=>{const n=s+10;setBest(bestScore("snake",n));return n});do{food.current={x:Math.floor(Math.random()*N),y:Math.floor(Math.random()*N)}}while(snake.current.some(p=>p.x===food.current.x&&p.y===food.current.y))}else snake.current.pop();draw()},Math.max(70,150-score*1.5));return()=>clearInterval(id)},[score,paused,over]);return <GameShell game={gameBySlug("snake")!} restart={reset}><div style={{width:"min(94vw,440px)",textAlign:"center"}}><div className="game-controls"><span className="stat">Score {score}</span><span className="stat">Best {best}</span><button className="btn secondary" onClick={()=>setPaused(x=>!x)}>{paused?"Resume":"Pause"}</button></div><canvas ref={canvas} width="400" height="400" onTouchStart={e=>touch.current=[e.touches[0].clientX,e.touches[0].clientY]} onTouchEnd={e=>{const dx=e.changedTouches[0].clientX-touch.current[0],dy=e.changedTouches[0].clientY-touch.current[1];Math.abs(dx)>Math.abs(dy)?setDirection(dx>0?1:-1,0):setDirection(0,dy>0?1:-1)}} style={{width:"100%",borderRadius:14,touchAction:"none"}}/><div className="game-controls"><button className="btn" onClick={()=>setDirection(0,-1)}>↑</button><button className="btn" onClick={()=>setDirection(-1,0)}>←</button><button className="btn" onClick={()=>setDirection(0,1)}>↓</button><button className="btn" onClick={()=>setDirection(1,0)}>→</button></div>{over&&<h2>Game Over</h2>}</div></GameShell>}
