"use client";import{useEffect,useRef,useState}from"react";import Link from"next/link";import{Game,games}from"@/data/games";import{getStored,setStored}from"@/lib/storage";import GameCard from"./GameCard";import AdPlaceholder from"./AdPlaceholder";
export default function GameShell({game,children,restart}:{game:Game;children:React.ReactNode;restart:()=>void}){const root=useRef<HTMLDivElement>(null);const[fav,setFav]=useState(false);useEffect(()=>{const fs=getStored<string[]>("favorites",[]);setFav(fs.includes(game.id));const rec=getStored<string[]>("recent",[]).filter(x=>x!==game.id);setStored("recent",[game.id,...rec].slice(0,8))},[game.id]);function favorite(){const fs=getStored<string[]>("favorites",[]);const next=fs.includes(game.id)?fs.filter(x=>x!==game.id):[...fs,game.id];setStored("favorites",next);setFav(next.includes(game.id))}async function share(){if(navigator.share)await navigator.share({title:game.title,url:location.href});else await navigator.clipboard.writeText(location.href)}return <div className="container" style={{paddingTop:24}} ref={root}><nav className="muted" style={{fontSize:14}}><Link href="/">Home</Link> / <Link href="/games">Games</Link> / {game.title}</nav><h1 style={{marginBottom:4}}>{game.title}</h1><p className="muted">{game.shortDescription}</p><div className="game-controls"><button className="btn secondary" onClick={()=>root.current?.requestFullscreen()}>⛶ Fullscreen</button><button className="btn secondary" onClick={restart}>↻ Restart</button><button className="btn secondary" onClick={favorite}>{fav?"♥ Favorited":"♡ Favorite"}</button><button className="btn secondary" onClick={share}>Share</button></div><div className="card game-stage">{children}</div><AdPlaceholder slot="game-bottom"/><section className="prose"><h2>How to Play</h2>{game.howTo.map(x=><p key={x}>• {x}</p>)}<h2>Controls</h2><p>{game.controls}</p><h2>Tips & Strategies</h2>{game.tips.map(x=><p key={x}>• {x}</p>)}<h2>About This Game</h2><p>{game.description} It runs entirely on your device, so there is nothing to install and no account to create.</p></section><AdPlaceholder slot="inline"/><section><h2>Related Games</h2><div className="grid-cards">{games.filter(g=>g.id!==game.id).slice(0,3).map(g=><GameCard key={g.id} game={g}/>)}</div></section><section className="prose" style={{marginTop:36}}><h2>FAQ</h2>{game.faq.map(x=><div key={x.q}><h3>{x.q}</h3><p>{x.a}</p></div>)}</section></div>}
