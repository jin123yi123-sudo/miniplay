"use client";import {useState} from "react";import {categories,games} from "@/data/games";import GameCard from "./GameCard";export default function GameBrowser(){const[q,setQ]=useState("");const[cat,setCat]=useState("All");const shown=games.filter(g=>(cat==="All"||g.category===cat)&&(`${g.title} ${g.tags.join(" ")}`.toLowerCase().includes(q.toLowerCase())));return <><div style={{display:"flex",gap:10,flexWrap:"wrap",margin:"22px 0"}}><input aria-label="Search games" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search games…" style={{padding:12,border:"1px solid var(--line)",borderRadius:12,flex:"1 1 220px"}}/><button className={`btn ${cat==="All"?"":"secondary"}`} onClick={()=>setCat("All")}>All</button>{categories.map(c=><button key={c} className={`btn ${cat===c?"":"secondary"}`} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="grid-cards">{shown.map(g=><GameCard key={g.id} game={g}/>)}</div></>}
