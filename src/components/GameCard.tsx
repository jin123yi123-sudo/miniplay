import Link from "next/link";import {Game} from "@/data/games";export default function GameCard({game}: {game:Game}){return <article className="card" style={{padding:18}}><div style={{height:120,borderRadius:14,display:"grid",placeItems:"center",fontSize:58,background:"linear-gradient(135deg,#eeeaff,#e8f7ff)"}}>{game.icon}</div><p className="muted" style={{fontSize:13,marginBottom:4}}>{game.category}</p><h3 style={{margin:"4px 0"}}>{game.title}</h3><p className="muted" style={{minHeight:42}}>{game.shortDescription}</p><Link className="btn" href={`/games/${game.slug}`} prefetch={false}>Play Now</Link></article>}
