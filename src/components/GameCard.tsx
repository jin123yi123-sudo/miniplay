import Link from'next/link';import type{Game}from'@/data/games';import{dictionaries}from'@/i18n/dictionary';import type{Locale}from'@/i18n/config';
const zhCategories={Arcade:'街机',Action:'动作','2 Player':'双人',Puzzle:'益智','Match-3':'三消',Casual:'休闲',Strategy:'策略',Brain:'脑力',Card:'纸牌'}as const;
export default function GameCard({game,locale='en'}:{game:Game;locale?:Locale}){const prefix=locale==='zh-CN'?'/zh':'',t=dictionaries[locale],category=locale==='zh-CN'?zhCategories[game.category]:game.category;return <article className="card" style={{padding:18}}><div style={{height:120,borderRadius:14,display:'grid',placeItems:'center',fontSize:58,background:game.cover||'linear-gradient(135deg,#eeeaff,#e8f7ff)',color:'white',textShadow:'0 2px 8px #0007'}}>{game.icon}</div><p className="muted" style={{fontSize:13,marginBottom:4}}>{category}</p><h3 style={{margin:'4px 0'}}>{game.title}</h3><p className="muted" style={{minHeight:42}}>{game.shortDescription}</p><Link className="btn" href={`${prefix}/games/${game.slug}`} prefetch={false}>{t.common.playNow}</Link></article>}
