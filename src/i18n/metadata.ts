import type{Metadata}from'next';import{absoluteUrl,site}from'@/config/site';import{languageAlternates,localizedPath,type Locale}from'./config';
export function localizedMetadata(locale:Locale,path:string,title:string,description:string):Metadata{const localPath=localizedPath(path,locale),url=absoluteUrl(localPath),languages=Object.fromEntries(Object.entries(languageAlternates(path)).map(([key,value])=>[key,absoluteUrl(value)]));return{title:{absolute:title},description,alternates:{canonical:url,languages},openGraph:{title:title.includes(site.name)?title:`${title} — ${site.name}`,description,url,siteName:site.name,type:'website',locale:locale==='zh-CN'?'zh_CN':'en_US'},twitter:{card:'summary',title,description}}}
