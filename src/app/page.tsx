import Link from "next/link";import {games,categories} from "@/data/games";import GameCard from "@/components/GameCard";import RecentlyPlayed from "@/components/RecentlyPlayed";export default function Home(){return <div className="container"><section style={{padding:"54px 0 26px"}}><span style={{color:"var(--brand)",fontWeight:800}}>NO DOWNLOADS. JUST PLAY.</span><h1 style={{fontSize:"clamp(2rem,6vw,4rem)",maxWidth:700,margin:"10px 0"}}>Quick browser games for short breaks.</h1><p className="muted" style={{fontSize:19,maxWidth:600}}>Five lightweight games, instant starts, and zero accounts. Pick one and make your next few minutes fun.</p></section><section><h2>Featured Games</h2><div className="grid-cards">{games.filter(g=>g.featured).map(g=><GameCard key={g.id} game={g}/>)}</div></section><RecentlyPlayed/><section style={{marginTop:42}}><h2>Browse Categories</h2><div style={{display:"flex",gap:10,flexWrap:"wrap"}}>{categories.map(c=><Link className="btn secondary" href={`/category/${c.toLowerCase()}`} key={c}>{c}</Link>)}</div></section><section style={{marginTop:42}}><h2>New Games</h2><div className="grid-cards">{games.filter(g=>g.isNew).map(g=><GameCard key={g.id} game={g}/>)}</div></section><section className="prose" style={{marginTop:48}}><h2>Small games, thoughtfully made</h2><p>MiniPlay is built for the moments between tasks. Every game opens directly in your browser, works with touch controls, and avoids heavy downloads. Our growing collection focuses on familiar rules, clear interfaces, and sessions that can be as short or long as you like.</p></section></div>}
