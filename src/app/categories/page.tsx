import Link from'next/link';import{categoryRegistry,games,getGameCategories}from'@/data/games';import{localizedMetadata}from'@/i18n/metadata';
export const metadata=localizedMetadata('en','/categories','Game Categories','Browse GameVado games by arcade, action, local multiplayer, puzzle, match-3, casual, and strategy categories.');
export default function Page(){return <div className="container" style={{paddingTop:40}}><h1>Game Categories</h1><p className="muted prose">Browse the collection by play style. A game may appear in more than one category when its mechanics genuinely fit.</p><div className="grid-cards">{categoryRegistry.map(category=>{const count=games.filter(game=>getGameCategories(game).includes(category.name)).length;return <article className="card" style={{padding:22}} key={category.slug}><h2>{category.name}</h2><p className="muted">{count?`${count} game${count===1?'':'s'}`:'Ready for future original games'}</p><Link className="btn secondary" href={`/category/${category.slug}`}>Browse {category.name}</Link></article>})}</div></div>}
