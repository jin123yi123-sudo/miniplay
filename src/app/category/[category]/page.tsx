import type{Metadata}from"next";import{notFound}from"next/navigation";import{categories,games}from"@/data/games";import GameCard from"@/components/GameCard";export function generateStaticParams(){return categories.map(c=>({category:c.toLowerCase()}))}export async function generateMetadata({params}:{params:Promise<{category:string}>}):Promise<Metadata>{const{category}=await params;const c=categories.find(x=>x.toLowerCase()===category);return{title:`${c} Games`,description:`Play free ${c} games online at MiniPlay.`,alternates:{canonical:`/category/${category}`}}}export default async function Page({params}:{params:Promise<{category:string}>}){const{category}=await params;const c=categories.find(x=>x.toLowerCase()===category);if(!c)notFound();return <div className="container" style={{paddingTop:40}}><h1>{c} Games</h1><p className="muted">Quick, free {c.toLowerCase()} games made for modern browsers.</p><div className="grid-cards">{games.filter(g=>g.category===c).map(g=><GameCard game={g} key={g.id}/>)}</div></div>}
