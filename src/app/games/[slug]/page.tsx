import type{Metadata}from"next";import{notFound}from"next/navigation";import{games,gameBySlug}from"@/data/games";import GameLoader,{type GameSlug}from"@/games/GameLoader";import{absoluteUrl}from"@/config/site";export const dynamicParams=false;export function generateStaticParams(){return games.map(g=>({slug:g.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const g=gameBySlug(slug);if(!g)return{};const url=absoluteUrl(`/games/${slug}`);return{title:g.seoTitle,description:g.seoDescription,alternates:{canonical:url},openGraph:{title:g.seoTitle,description:g.seoDescription,url,type:"website"},twitter:{card:"summary",title:g.seoTitle,description:g.seoDescription}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=gameBySlug(slug);if(!g)notFound();const url=absoluteUrl(`/games/${g.slug}`),schema=[{"@context":"https://schema.org","@type":"VideoGame",name:g.title,description:g.description,url,applicationCategory:"Game",operatingSystem:"Any",isAccessibleForFree:true},{"@context":"https://schema.org","@type":"BreadcrumbList",itemListElement:[{"@type":"ListItem",position:1,name:"Home",item:absoluteUrl("/")},{"@type":"ListItem",position:2,name:"Games",item:absoluteUrl("/games")},{"@type":"ListItem",position:3,name:g.title,item:url}]}];return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><GameLoader slug={g.slug as GameSlug}/></>}
