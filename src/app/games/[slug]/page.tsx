import type{Metadata}from"next";import{notFound}from"next/navigation";import{games,gameBySlug}from"@/data/games";import GameLoader from"@/games/GameLoader";import{site}from"@/config/site";export function generateStaticParams(){return games.map(g=>({slug:g.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const g=gameBySlug(slug);if(!g)return{};return{title:g.seoTitle,description:g.seoDescription,alternates:{canonical:`/games/${slug}`},openGraph:{title:g.seoTitle,description:g.seoDescription,url:`/games/${slug}`}}}export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=gameBySlug(slug);if(!g)notFound();const schema={"@context":"https://schema.org","@type":"VideoGame",name:g.title,description:g.description,url:`${site.url}/games/${g.slug}`,applicationCategory:"Game",operatingSystem:"Any",isAccessibleForFree:true};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><GameLoader slug={g.slug as "number-merge"|"snake"|"sudoku"|"solitaire"|"emoji-memory"}/></>}
