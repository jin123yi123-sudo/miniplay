import Link from'next/link';import GameCard from'@/components/GameCard';import{games}from'@/data/games';import{localizedMetadata}from'@/i18n/metadata';
export const metadata=localizedMetadata('en','/games/match-3','Match-3 Games','Explore seven original GameVado match-3 games with distinct themes, 30 levels each, and separate progress.');
export default function Page(){const titles=games.filter(g=>g.tags.includes('match-3'));return <div className="container" style={{paddingTop:40}}><nav className="muted" aria-label="Breadcrumb"><Link href="/">Home</Link> / <Link href="/games">Games</Link> / Match-3</nav><h1>Match-3 Games</h1><p className="muted prose">Seven independent worlds share clear, strategic matching rules while keeping their own artwork, language, analytics identity, and saved progress. Pick a theme below—none requires a download or account.</p><div className="grid-cards">{titles.map(game=><GameCard key={game.id} game={game}/>)}</div><section className="prose" style={{marginTop:40}}><h2>One Rule Set, Seven Adventures</h2><p>Swap adjacent pieces to form groups of three. Matches of four or five and intersecting shapes create themed specials, while cascades reward planning beyond the first clear. Every title offers thirty move-limited levels with score, collection, and obstacle goals.</p><h2>Progress That Stays Independent</h2><p>Completing a level in one adventure never unlocks another. Each game records its own stars and best scores in this browser, so you can explore every theme from Level 1 or focus on one campaign.</p></section></div>}
