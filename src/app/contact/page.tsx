import type{Metadata}from"next";import{site}from"@/config/site";export const metadata:Metadata={title:"Contact",description:"Contact the MiniPlay team.",alternates:{canonical:"/contact"}};export default function Page(){return <article className="container prose" style={{paddingTop:44}}><h1>Contact</h1>{site.email?<p>Questions, accessibility feedback, or game suggestions are welcome. Email us at <a href={`mailto:${site.email}`}>{site.email}</a>.</p>:<p>Our public support inbox is being prepared. Please check this page again after the production domain launches.</p>}<p>MiniPlay does not use a contact form or collect submissions on this website.</p></article>}
