import{site}from'@/config/site';import{localizedMetadata}from'@/i18n/metadata';export const metadata=localizedMetadata('en','/contact','Contact','Contact the GameVado team.');export default function Page(){return <article className="container prose" style={{paddingTop:44}}><h1>Contact</h1>{site.email?<p>Questions, accessibility feedback, or game suggestions are welcome. Email us at <a href={`mailto:${site.email}`}>{site.email}</a>.</p>:<p>Our public support inbox is being prepared. Please check this page again after the production domain launches.</p>}<p>{site.name} does not use a contact form or collect submissions on this website.</p></article>}
