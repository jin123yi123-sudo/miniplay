import type{MetadataRoute}from"next";import{absoluteUrl}from"@/config/site";import{games,categories}from"@/data/games";const updated=new Date("2026-08-28");export default function sitemap():MetadataRoute.Sitemap{const paths=["/","/games","/about","/contact","/privacy","/terms",...games.map(g=>`/games/${g.slug}`),...categories.map(c=>`/category/${c.toLowerCase()}`)];return paths.map(path=>({url:absoluteUrl(path),lastModified:updated,changeFrequency:path.startsWith("/games/")?"monthly":"weekly",priority:path==="/"?1:.7}))}
