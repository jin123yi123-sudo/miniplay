import type{MetadataRoute}from"next";import{site}from"@/config/site";import{games,categories}from"@/data/games";export default function sitemap():MetadataRoute.Sitemap{const paths=["","/games","/about","/contact","/privacy","/terms",...games.map(g=>`/games/${g.slug}`),...categories.map(c=>`/category/${c.toLowerCase()}`)];return paths.map(path=>({url:site.url+path,lastModified:new Date(),changeFrequency:path.startsWith("/games/")?"monthly":"weekly",priority:path===""?1:.7}))}
