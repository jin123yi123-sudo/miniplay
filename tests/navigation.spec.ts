import{expect,test}from"./fixtures";
const gameSlugs=["gem-match","fruit-match","pet-match","dessert-match","ocean-match","space-match","anime-match","2048-merge","tank-duel","pixel-fighters","galaxy-strike","neon-runner","fruit-slash","number-merge","snake","sudoku","solitaire","emoji-memory","minesweeper","tic-tac-toe","pong","breakout","connect-four"];
const categories=["puzzle","arcade","brain","card","casual"];
test("public routes load without horizontal page overflow",async({page})=>{for(const path of["/","/games","/games/match-3",...categories.map(value=>`/category/${value}`),"/privacy","/terms","/about","/contact"]){await page.goto(path);await expect(page.locator("h1")).toBeVisible();const overflow=await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth+1);expect(overflow,`${path} has horizontal overflow`).toBeFalsy()}});
test("all game pages load",async({page})=>{for(const slug of gameSlugs){await page.goto(`/games/${slug}`);await expect(page.locator("h1")).toBeVisible();await expect(page.locator(".game-stage")).toBeVisible()}});
test("unknown routes return a real 404",async({page})=>{for(const path of["/games/does-not-exist","/does-not-exist"]){const response=await page.goto(path);expect(response?.status()).toBe(404);await expect(page.getByRole("heading",{name:"Page not found"})).toBeVisible()}});
